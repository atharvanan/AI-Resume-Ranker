const express = require('express');
const router = express.Router();
// Currently job processing is handled in /analyze with resumes, but we could split it later.
// router.post('/upload', ...);

module.exports = router;
