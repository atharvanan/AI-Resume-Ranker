{
  "buildCommand": "npm run build --prefix client",
  "outputDirectory": "client/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://your-render-backend-url.com/api/:path*"
    }
  ]
}
